# frontend outline (draft)

## inputs

### environment vars

- `$FRONTEND_PORT`: port on which the frontend process will listen
- `$MONGO_URL`: URL pointing to the local MongoDB the frontend will monitor
